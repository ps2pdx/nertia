import { Gallery } from "./Gallery";

export const metadata = {
  title: "Templates — nertia",
  description: "Pick a template, fill in your copy, ship.",
};

export default function TemplatesPage() {
  return <Gallery />;
}
