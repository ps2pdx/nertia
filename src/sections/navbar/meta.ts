import type { SectionMeta } from "../types";

export const meta: SectionMeta = {
    id: "navbar",
    displayName: "Navbar",
    family: "navbar",
    tags: ["navbar", "top-nav", "wordmark", "chrome"],
};
