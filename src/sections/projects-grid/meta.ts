import type { SectionMeta } from "../types";

export const meta: SectionMeta = {
    id: "projects-grid",
    displayName: "Projects grid",
    family: "content",
    tags: ["projects", "grid", "portfolio", "work", "gallery", "showcase"],
};
