import type { SectionMeta } from "../types";

export const meta: SectionMeta = {
    id: "marketing-hero",
    displayName: "Marketing hero",
    family: "hero",
    tags: ["hero", "marketing", "product", "saas", "landing", "centered", "above-fold"],
};
