import type { SectionMeta } from "../types";

export const meta: SectionMeta = {
    id: "about",
    displayName: "About",
    family: "content",
    tags: ["about", "bio", "personal", "story", "content"],
};
