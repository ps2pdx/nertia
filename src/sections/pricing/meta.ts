import type { SectionMeta } from "../types";

export const meta: SectionMeta = {
    id: "pricing",
    displayName: "Pricing",
    family: "content",
    tags: ["pricing", "tiers", "saas", "product", "business", "subscription"],
};
