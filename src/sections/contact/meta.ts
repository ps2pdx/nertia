import type { SectionMeta } from "../types";

export const meta: SectionMeta = {
    id: "contact",
    displayName: "Contact",
    family: "contact",
    tags: ["contact", "reach-out", "cta", "inquiry", "hire"],
};
