import type { SectionMeta } from "../types";

export const meta: SectionMeta = {
    id: "post-list",
    displayName: "Post list",
    family: "posts",
    tags: ["blog", "writer", "newsletter", "posts", "editorial", "writing", "journal", "essays"],
};
